# typtop common-auth
auth    sufficient   /usr/local/lib/security/libpam_typtop.so try_first_pass first_time
# --> typtop is not initialized or, password not in tytop db (=> new password or copletely wrong password).
auth    requisite     pam_unix.so nullok_secure try_first_pass
auth    sufficient   /usr/local/lib/security/libpam_typtop.so try_first_pass second_time   # Always returns true
# --> The correct password entered and typtop is not initialized, OR
# --> re-initialize typtop (keep the salt).
auth    requisite                       pam_deny.so
auth    required                        pam_permit.so